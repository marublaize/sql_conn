apiVersion: v1
kind: Service
metadata:
    name: "expresscosmosdb-aff3"
    labels:
        app: "expresscosmosdb-aff3"
spec:
    type: ClusterIP
    ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
    selector:
        app: "expresscosmosdb-aff3"